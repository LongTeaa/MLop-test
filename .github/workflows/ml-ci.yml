name: Pipeline Build và Test Model

on:
  push:
    branches: [ main, master ]
    paths:
      - 'lab3/**'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'lab3/**'

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: lab3

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Train model
      run: |
        python train.py

    - name: Run model tests (Quality Gate)
      run: |
        pytest test_model.py

    - name: Upload trained model artifact
      uses: actions/upload-artifact@v3
      with:
        name: trained-model
        path: lab3/models/model.pkl
        retention-days: 7